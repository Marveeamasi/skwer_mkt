export type Reward={sellerId:string;ownerPhone:string;amountKobo:number;status:"pending"|"available"|"redeemed"|"expired"|"reversed"|"cancelled";expiresAt:Date|null};export function validateReward(input:{reward:Reward;sellerId:string;buyerPhone:string;orderSubtotalKobo:number;hasReferral:boolean}){const r=input.reward;if(r.status!=="available")return {valid:false,reason:"Reward is not available"} as const;if(r.sellerId!==input.sellerId)return {valid:false,reason:"Reward belongs to another seller"} as const;if(r.ownerPhone!==input.buyerPhone)return {valid:false,reason:"Use the phone number that earned this reward"} as const;if(r.expiresAt&&r.expiresAt<=new Date())return {valid:false,reason:"Reward has expired"} as const;if(input.hasReferral)return {valid:false,reason:"Rewards cannot be stacked with a referral"} as const;return {valid:true,amountKobo:Math.min(r.amountKobo,input.orderSubtotalKobo)} as const}export function isDistinctReferral(a:{phone:string;email:string},b:{phone:string;email:string}){return a.phone!==b.phone&&a.email.toLowerCase()!==b.email.toLowerCase()}
