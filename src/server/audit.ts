import "server-only";import {createHash} from "node:crypto";import {createAdminClient} from "@/lib/supabase/admin";export async function audit(input:{actorId?:string;action:string;resourceType:string;resourceId?:string;before?:unknown;after?:unknown;request?:Request}){const ip=input.request?.headers.get("x-forwarded-for")?.split(",")[0]?.trim();const ipHash=ip?createHash("sha256").update(ip).digest("hex"):null;const {error}=await createAdminClient().from("audit_logs").insert({actor_profile_id:input.actorId??null,action:input.action,resource_type:input.resourceType,resource_id:input.resourceId??null,before_data:input.before??null,after_data:input.after??null,ip_hash:ipHash,user_agent:input.request?.headers.get("user-agent")?.slice(0,500)??null});if(error)throw error}
