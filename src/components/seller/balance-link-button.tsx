"use client";
import {useState} from "react";
import {Copy,Link2} from "lucide-react";
export function BalanceLinkButton({orderId}:{orderId:string}){const[busy,setBusy]=useState(false),[url,setUrl]=useState(""),[error,setError]=useState("");async function create(){setBusy(true);setError("");try{const response=await fetch(`/api/seller/orders/${orderId}/balance-link`,{method:"POST"}),result=await response.json();if(!response.ok)throw new Error(result.error);setUrl(result.url);await navigator.clipboard.writeText(result.url)}catch(caught){setError(caught instanceof Error?caught.message:"Could not create balance link")}finally{setBusy(false)}}async function copy(){await navigator.clipboard.writeText(url)}return <div className="form-stack">{url?<><p className="notice">Balance link created and copied. It expires in seven days.</p><button type="button" className="button button-small" onClick={copy}><Copy size={16}/> Copy balance link</button></>:<button type="button" className="button button-small" onClick={create} disabled={busy}><Link2 size={16}/>{busy?"Creating…":"Create balance link"}</button>}{error&&<p className="form-error" role="alert">{error}</p>}</div>}
