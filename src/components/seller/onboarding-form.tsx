"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";export function OnboardingForm(){const [busy,setBusy]=useState(false),[error,setError]=useState(""),router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget),response=await fetch("/api/seller/onboarding",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({businessName:f.get("businessName"),whatsappPhone:f.get("whatsappPhone"),category:f.get("category"),city:f.get("city"),state:f.get("state"),shortDescription:f.get("shortDescription"),pickupNote:f.get("pickupNote"),deliveryNote:f.get("deliveryNote"),returnPolicy:f.get("returnPolicy"),termsAccepted:f.get("termsAccepted")==="on"})}),body=await response.json();setBusy(false);if(!response.ok){setError(body.error);return}router.push("/seller/settings?step=payments")}return <form className="panel form-stack" onSubmit={submit}><label>Business name<input name="businessName" required/></label><label>WhatsApp phone<input name="whatsappPhone" required inputMode="tel"/></label><label>Product category<select name="category"><option>Fashion</option><option>Beauty</option><option>Home</option><option>Accessories</option></select></label><div className="settings-grid"><label>City<input name="city" required defaultValue="Port Harcourt"/></label><label>State<input name="state" required defaultValue="Rivers"/></label></div><label>Short trust description<textarea name="shortDescription" required minLength={10} maxLength={240}/></label><label>Pickup location text<textarea name="pickupNote"/></label><label>Default delivery note<textarea name="deliveryNote"/></label><label>Return/refund policy<textarea name="returnPolicy" required minLength={10}/></label><label className="checkbox"><input name="termsAccepted" type="checkbox" required/> I accept the seller terms and prohibited products policy.</label><button className="button" disabled={busy}>{busy?"Saving…":"Save and add payment details"}</button>{error&&<p className="form-error" role="alert">{error}</p>}</form>}
