"use client";
import {useState} from "react";
import Link from "next/link";
import {useRouter} from "next/navigation";
export function StatusForm({resource,id,current,options}:{resource:"campaigns"|"orders";id:string;current:string;options:string[]}){const[value,setValue]=useState(current),[busy,setBusy]=useState(false),[error,setError]=useState(""),router=useRouter();async function save(){setBusy(true);setError("");const response=await fetch(`/api/seller/${resource}/${id}/status`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({status:value})});const result=await response.json();setBusy(false);if(response.ok)router.refresh();else setError(result.error??"Status could not be updated")}return <><div className="hero-actions"><select className="app-input" value={value} onChange={event=>setValue(event.target.value)} aria-label="Status">{options.map(option=><option value={option} key={option}>{option.replaceAll("_"," ")}</option>)}</select><button className="button button-small" disabled={busy||value===current} onClick={save}>{busy?"Saving…":"Save status"}</button>{resource==="orders"&&<Link className="button button-small button-secondary" href={`/seller/orders/${id}/refund`}>Refund</Link>}</div>{error&&<p className="form-error" role="alert">{error}</p>}</>}
