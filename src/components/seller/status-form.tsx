"use client";import {useState} from "react";import {useRouter} from "next/navigation";export function StatusForm({resource,id,current,options}:{resource:"campaigns"|"orders";id:string;current:string;options:string[]}){const [value,setValue]=useState(current),[busy,setBusy]=useState(false),router=useRouter();async function save(){setBusy(true);const response=await fetch(`/api/seller/${resource}/${id}/status`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({status:value})});setBusy(false);if(response.ok)router.refresh()}return <div className="hero-actions"><select className="app-input" value={value} onChange={e=>setValue(e.target.value)} aria-label="Status">{options.map(x=><option value={x} key={x}>{x.replaceAll("_"," ")}</option>)}</select><button className="button button-small" disabled={busy||value===current} onClick={save}>{busy?"Saving…":"Save status"}</button></div>}
