"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
import {PasswordInput} from "@/components/auth/password-input";
type Details={fullName:string;email:string;password:string};
export function RegisterForm(){const router=useRouter(),[step,setStep]=useState<"details"|"otp">("details"),[details,setDetails]=useState<Details|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");async function requestCode(email:string){const response=await fetch("/api/auth/request-otp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})}),result=await response.json();if(!response.ok)throw new Error(result.error)}async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);try{if(step==="details"){const next={fullName:String(form.get("fullName")).trim(),email:String(form.get("email")).trim().toLowerCase(),password:String(form.get("password"))};await requestCode(next.email);setDetails(next);setStep("otp")}else{if(!details)throw new Error("Enter your account details again.");const response=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...details,code:String(form.get("code"))})}),result=await response.json();if(!response.ok)throw new Error(result.error);router.push("/login?registered=1")}}catch(caught){setError(caught instanceof Error?caught.message:"Something went wrong")}finally{setBusy(false)}}async function resend(){if(!details)return;setBusy(true);setError("");try{await requestCode(details.email)}catch(caught){setError(caught instanceof Error?caught.message:"Code could not be resent")}finally{setBusy(false)}}return <form className="form-stack" onSubmit={submit}>{step==="details"?<><label>Full name<input name="fullName" required minLength={2} maxLength={80} autoComplete="name"/></label><label>Email address<input name="email" required type="email" maxLength={254} autoComplete="email"/></label><PasswordInput autoComplete="new-password" minLength={8} maxLength={72}/><small>Use at least 8 characters. Your password is never emailed.</small><button className="button" disabled={busy}>{busy?"Sending code…":"Send verification code"}</button></>:<><div className="notice">We sent a 6-digit code to <strong>{details?.email}</strong>. It expires in 10 minutes.</div><label>Verification code<input name="code" required inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="one-time-code"/></label><button className="button" disabled={busy}>{busy?"Creating account…":"Verify and create account"}</button><button type="button" className="link-button" onClick={resend} disabled={busy}>Send a new code</button><button type="button" className="link-button" onClick={()=>{setStep("details");setDetails(null);setError("")}}>Change details</button></>}{error&&<p className="form-error" role="alert">{error}</p>}</form>}
