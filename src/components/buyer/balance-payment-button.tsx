"use client";
import {useState} from "react";
export function BalancePaymentButton({token}:{token:string}){const[busy,setBusy]=useState(false),[error,setError]=useState("");async function pay(){setBusy(true);setError("");try{const response=await fetch("/api/payments/balance/initialize",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token})}),result=await response.json();if(!response.ok)throw new Error(result.error);location.assign(result.authorizationUrl)}catch(caught){setError(caught instanceof Error?caught.message:"Payment could not start");setBusy(false)}}return <><button className="button" onClick={pay} disabled={busy}>{busy?"Preparing secure payment…":"Pay outstanding balance"}</button>{error&&<p className="form-error" role="alert">{error}</p>}</>}
