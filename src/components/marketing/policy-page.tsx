import Link from "next/link";import {publicConfig} from "@/lib/config";export function PolicyPage({title,summary,sections}:{title:string;summary:string;sections:{title:string;body:string}[]}){return <main className="public-page"><nav className="public-nav shell"><Link className="brand" href="/"><span className="brand-mark">↗</span>{publicConfig.NEXT_PUBLIC_APP_NAME}</Link><Link href="/terms">Legal centre</Link></nav><article className="policy-wrap"><span className="eyebrow">Last draft update · 18 July 2026</span><h1>{title}</h1><p className="legal-draft"><strong>Draft for professional review:</strong> This operational placeholder is not a substitute for Nigerian legal, privacy, accounting, consumer-protection or payment-provider advice. It must be approved before live launch.</p><p>{summary}</p>{sections.map(s=><section key={s.title}><h2>{s.title}</h2><p>{s.body}</p></section>)}<h2>Questions and rights requests</h2><p>Contact <a href={`mailto:${publicConfig.NEXT_PUBLIC_SUPPORT_EMAIL}`}>{publicConfig.NEXT_PUBLIC_SUPPORT_EMAIL}</a>. We will verify requests before disclosing or changing protected information.</p></article></main>}
