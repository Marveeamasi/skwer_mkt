import Link from "next/link";import {publicConfig} from "@/lib/config";export default function AuthLayout({children}:{children:React.ReactNode}){return <main className="auth-page"><Link className="brand" href="/"><span className="brand-mark">↗</span>{publicConfig.NEXT_PUBLIC_APP_NAME}</Link><section className="auth-card">{children}</section><p className="auth-foot">Secure selling tools for Nigerian WhatsApp businesses.</p></main>}
