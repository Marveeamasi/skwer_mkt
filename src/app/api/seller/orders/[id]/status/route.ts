import {NextResponse} from "next/server";import {z} from "zod";import {createClient} from "@/lib/supabase/server";import {createAdminClient} from "@/lib/supabase/admin";import {canTransition} from "@/features/orders/states";import {audit} from "@/server/audit";import {qualifyReferralReward} from "@/server/growth";const schema=z.object({status:z.string()});export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,{status}=schema.parse(await request.json()),session=await createClient(),{data:claims}=await session.auth.getClaims(),userId=claims?.claims?.sub;if(!userId)return NextResponse.json({error:"Sign in required"},{status:401});const {data:owned}=await session.from("orders").select("id,status").eq("id",id).single();if(!owned)return NextResponse.json({error:"Order not found"},{status:404});if(!canTransition(owned.status,status))return NextResponse.json({error:"That status change is not allowed"},{status:409});const admin=createAdminClient(),{error}=await admin.from("orders").update({status}).eq("id",id);if(error)throw error;await admin.from("order_events").insert({order_id:id,event_type:"status_changed",from_status:owned.status,to_status:status,actor_type:"seller",actor_id:userId,public_message:`Order ${status.replaceAll("_"," ")}`});if(status==="delivered"||status==="picked_up")await qualifyReferralReward(id);await audit({actorId:userId,action:"order.status_changed",resourceType:"order",resourceId:id,before:{status:owned.status},after:{status},request});return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Could not update status"},{status:400})}}
