import {createClient} from "@/lib/supabase/server";
function cell(value:unknown){return `"${String(value??"").replaceAll('"','""')}"`}
export async function GET(){const db=await createClient(),{data}=await db.from("orders").select("public_reference,status,fulfilment_method,total_due_kobo,total_paid_kobo,created_at,customer:customers(full_name,normalized_phone)").order("created_at",{ascending:false}),header=["reference","buyer","phone","status","fulfilment","total_due_kobo","total_paid_kobo","created_at"],lines=[header.map(cell).join(","),...(data??[]).map(row=>{const raw=row.customer as unknown,customer=(Array.isArray(raw)?raw[0]:raw) as {full_name?:string;normalized_phone?:string}|null;return [row.public_reference,customer?.full_name,customer?.normalized_phone,row.status,row.fulfilment_method,row.total_due_kobo,row.total_paid_kobo,row.created_at].map(cell).join(",")})];return new Response(lines.join("\r\n"),{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":`attachment; filename="skwer-orders-${new Date().toISOString().slice(0,10)}.csv"`,"cache-control":"private, no-store"}})}
