import {NextResponse} from "next/server";import {z} from "zod";import {createClient} from "@/lib/supabase/server";import {audit} from "@/server/audit";const schema=z.object({status:z.enum(["active","paused","sold_out","ended","archived"])});export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,{status}=schema.parse(await request.json()),db=await createClient(),{data:claims}=await db.auth.getClaims(),userId=claims?.claims?.sub;if(!userId)return NextResponse.json({error:"Sign in required"},{status:401});const {data:before}=await db.from("campaigns").select("status").eq("id",id).single(),{error}=await db.from("campaigns").update({status}).eq("id",id);if(error)throw error;await audit({actorId:userId,action:"campaign.status_changed",resourceType:"campaign",resourceId:id,before,after:{status},request});return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Could not update status"},{status:400})}}
