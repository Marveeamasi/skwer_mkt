import { featureFlags } from "@/lib/config";
export default function Page(){const flags=[["Payments",featureFlags.payments],["Split settlements",featureFlags.splitPayments],["Rewards",featureFlags.rewards],["Deposits",featureFlags.deposits],["Restock requests",featureFlags.restock]] as const;return <><header className="app-topbar"><div><h1>Platform settings</h1><p>Effective server launch gates from the deployed environment.</p></div></header><section className="panel"><div className="order-summary">{flags.map(([name,enabled])=><p key={name}><span>{name}</span><strong>{enabled?"Enabled":"Disabled"}</strong></p>)}</div><p className="notice">These security-sensitive gates are deployment configuration, not browser controls. Changes require an audited deployment and provider approval where applicable.</p></section></>}
