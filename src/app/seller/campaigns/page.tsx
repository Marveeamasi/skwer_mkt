import Link from "next/link";import {PackagePlus} from "lucide-react";import {formatNaira} from "@/lib/money";import {createClient} from "@/lib/supabase/server";interface CampaignRow{id:string;short_code:string;status:string;public_price_kobo:number;product:{name:string}|{name:string}[]|null}
export default async function Page(){const db=await createClient(),{data}=await db.from("campaigns").select("id,short_code,status,public_price_kobo,product:products(name)").order("created_at",{ascending:false}),rows=(data??[]) as unknown as CampaignRow[];return <><header className="app-topbar"><div><h1>Sales links</h1><p>Create, share and manage one product at a time.</p></div><Link className="button button-small" href="/seller/campaigns/new"><PackagePlus size={17}/> New sales link</Link></header>{rows.length?<section className="panel table-wrap"><table className="data-table"><thead><tr><th>Product</th><th>Status</th><th>Buyer sees</th><th>Sales link</th><th></th></tr></thead><tbody>{rows.map(r=>{const p=Array.isArray(r.product)?r.product[0]:r.product;return <tr key={r.id}><td><strong>{p?.name??"Unnamed product"}</strong></td><td><span className="badge">{r.status.replaceAll("_"," ")}</span></td><td>{formatNaira(Number(r.public_price_kobo))}</td><td><Link href={`/p/${r.short_code}`}>/p/{r.short_code}</Link></td><td><Link href={`/seller/campaigns/${r.id}`}>Manage →</Link></td></tr>})}</tbody></table></section>:<section className="panel empty-state"><PackagePlus size={38}/><h2>No sales links yet</h2><p>Add one product and share it on WhatsApp today.</p><Link className="button button-small" href="/seller/campaigns/new">Create sales link</Link></section>}</>}
