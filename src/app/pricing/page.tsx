import Link from "next/link";import {Check} from "lucide-react";import {publicConfig} from "@/lib/config";export const metadata={title:"Pricing"};export default function Page(){return <main className="public-page"><nav className="public-nav shell"><Link className="brand" href="/"><span className="brand-mark">↗</span>{publicConfig.NEXT_PUBLIC_APP_NAME}</Link><Link href="/login">Seller login</Link></nav><section className="policy-wrap"><span className="eyebrow">Pay only when a sale occurs</span><h1>No setup fee. No monthly subscription in Version 1.</h1><p className="lede">Tell us how much you must receive. Your buyer sees one inclusive item price—without a surprise platform fee at checkout.</p><section className="panel"><h2>Included with each successful order</h2>{["Secure Paystack checkout","Automatic payment and order connection","Stock reservation","Private buyer tracking link","Referral attribution and funded buyer reward","Sales, customer and profit-estimate records"].map(x=><p key={x}><Check size={17} color="#15803d" style={{display:"inline",marginRight:8}}/>{x}</p>)}<p className="notice">The exact seller breakdown is shown before publishing and stored with every historical order. Payment-provider costs and platform fees are versioned admin configuration.</p><Link className="button" href="/register">Create a test sales link</Link></section></section></main>}
