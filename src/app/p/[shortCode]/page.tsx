import {notFound} from "next/navigation";import type {Metadata} from "next";import Link from "next/link";import {ProductPurchase} from "@/components/buyer/product-purchase";import {getPublicCampaign} from "@/features/campaigns/public-campaign";import {publicConfig} from "@/lib/config";import {formatNaira} from "@/lib/money";
export async function generateMetadata({params}:{params:Promise<{shortCode:string}>}):Promise<Metadata>{const {shortCode}=await params,c=await getPublicCampaign(shortCode);if(!c)return {};const title=`${c.product.name} from ${c.seller.businessName}`,description=`Buy for ${formatNaira(c.publicPriceKobo)}. Secure checkout and order tracking.`;return {title,description,robots:c.noindex?{index:false,follow:false}:undefined,alternates:{canonical:`/p/${shortCode}`},openGraph:{title,description,url:`/p/${shortCode}`,images:[`/p/${shortCode}/opengraph-image`]},twitter:{card:"summary_large_image",title,description}}}
export default async function Page({params}:{params:Promise<{shortCode:string}>}){const {shortCode}=await params,c=await getPublicCampaign(shortCode);if(!c)notFound();return <main className="public-page"><nav className="public-nav shell"><Link className="brand" href="/"><span className="brand-mark">↗</span>{publicConfig.NEXT_PUBLIC_APP_SHORT_NAME}</Link><div className="seller-chip"><span>{c.seller.businessName.slice(0,2).toUpperCase()}</span><div><strong>{c.seller.businessName}</strong><br/><small>{c.seller.city}{c.seller.paymentVerified?" · Verified payment account":""}</small></div></div></nav><section className="product-layout shell"><div className="product-gallery"><div className="product-hero"><span>Sold by {c.seller.businessName}</span></div><p className="notice">Seller policy: {c.seller.returnPolicy}</p></div><ProductPurchase campaign={c}/></section></main>}
