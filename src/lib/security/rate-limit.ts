import "server-only";import {createHash} from "node:crypto";import {createAdminClient} from "@/lib/supabase/admin";export async function enforceRateLimit(input:{scope:string;identifier:string;limit:number;windowSeconds:number}){const hash=createHash("sha256").update(`${input.scope}:${input.identifier}`).digest("hex"),{data,error}=await createAdminClient().rpc("consume_rate_limit",{p_key_hash:hash,p_limit:input.limit,p_window_seconds:input.windowSeconds});if(error)throw new Error("Rate limiter is unavailable");if(!data)throw new RateLimitError()}export class RateLimitError extends Error{status=429;constructor(){super("Too many attempts. Please wait and try again.")}}
