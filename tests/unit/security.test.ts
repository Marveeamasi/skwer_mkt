import {describe,expect,it} from "vitest";import {createOpaqueToken,tokenMatches} from "@/lib/security/tokens";import {createHmac} from "node:crypto";import {verifyPaystackSignature} from "@/lib/paystack/signature";describe("tokens",()=>{it("checks hash",()=>{const {token,hash}=createOpaqueToken();expect(tokenMatches(token,hash)).toBe(true);expect(tokenMatches(token+"x",hash)).toBe(false)})});describe("webhook",()=>{it("uses raw body",()=>{const b='{"event":"charge.success"}',s="secret",h=createHmac("sha512",s).update(b).digest("hex");expect(verifyPaystackSignature(b,h,s)).toBe(true);expect(verifyPaystackSignature(b+" ",h,s)).toBe(false)})});
